import { requireSession } from '@/lib/auth';
import { ApiForm } from '@/components/Forms';
export default async function OnboardingPage() { await requireSession(); return <main className="appliance-bg grid min-h-screen place-items-center p-6"><div className="w-full max-w-xl"><h1 className="mb-3 font-display text-6xl">Name your household</h1><p className="mb-6 text-lg text-ink/65">This creates the local household scope every profile, event, list, meal, and device belongs to.</p><ApiForm endpoint="/api/onboarding" redirectTo="/app" submitLabel="Create household" fields={[{ name: 'householdName', label: 'Household name', defaultValue: 'Santangelo Family', required: true }, { name: 'timezone', label: 'Timezone', defaultValue: 'America/New_York', required: true }]} /></div></main>; }
